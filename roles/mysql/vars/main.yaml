# MY_SQL_ROOTPWD: "{{ lookup('amazon.aws.aws_ssm', '/roboshop/mysql/mysql_rootpassword', region='us-east-1', decrypt= True) }}"

MYSQL_ROOT_PASSWORD: "{{ lookup('amazon.aws.aws_ssm', '/roboshop/{{ env }}/mysql/mysql_rootpassword', region='us-east-1', decrypt= True) }}"